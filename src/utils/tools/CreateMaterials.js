import * as THREE from "three"
export const normal = new THREE.MeshPhongMaterial({
    color: new THREE.Color(0xffffff),
    name: "normal",
});
export const danger = new THREE.MeshPhongMaterial({
    color: new THREE.Color(0xeb191f),
    name: "danger",
});

